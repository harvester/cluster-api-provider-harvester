apiVersion: apps/v1
kind: Deployment
metadata:
  name: caphv-controller-manager
  namespace: caphv-system
spec:
  template:
    spec:
      containers:
      - name: manager
        imagePullPolicy: Always