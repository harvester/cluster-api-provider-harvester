apiVersion: apps/v1
kind: Deployment
metadata:
  name: caphv-controller-manager
  namespace: caphv-system
spec:
  template:
    spec:
      containers:
      - name: manager
        image: ghcr.io/rancher-sandbox/cluster-api-provider-harvester:dev
